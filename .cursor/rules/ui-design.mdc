---
description: 现代 Figma 科技风 UI 设计规范 — 黑墨绿 + 翠绿配色
globs: frontend/**/*.{tsx,css}
alwaysApply: false
---

# UI 设计规范 · 黑墨绿科技风

Cloud Travel Guide 的界面呈现 **近黑墨绿底 + 翠绿强调** 的科技 HUD 感，参考现代 Figma 设计系统（变量驱动、组件化、层次清晰）。

## 设计原则

1. **内容优先**：3D 场景占满视口，UI 以浮动面板/控件叠加，不遮挡核心画面。
2. **层次靠透明度**：用 `bg-surface-*/70` + `backdrop-blur` 区分层级，避免实心大块色块。
3. **数据可读**：坐标、状态等用 `font-mono` + 小字号；说明文字用 `text-ink-300/400`。
4. **克制动效**：`transition` 150–200ms；悬停仅改背景/边框，不做夸张缩放。

## 色彩与 Token

**禁止**使用 `slate-*` 或散落 hex；一律使用 `globals.css` `@theme` 中的语义 token：

| 用途 | Tailwind 类 | 色感 |
|------|-------------|------|
| 页面底 | `bg-surface-950` | 近黑墨绿 `#030a06` |
| 面板底 | `bg-surface-900/70` ~ `bg-surface-800/70` | 墨绿半透明 |
| 主强调 / 选中 | `bg-brand-600` / `text-brand-500` | 翠绿 `#059669` / `#10b981` |
| 高亮 / 指示 | `text-brand-400` / `bg-brand-600/80` | 亮翠绿 `#34d399` |
| 正文 | `text-ink-100` ~ `text-ink-300` | 绿调浅字 |
| 次要/提示 | `text-ink-400` | 灰绿 `#5f9d82` |
| 边框微光 | `ring-brand-500/10` / `border-surface-700` | 翠绿淡描边 |
| 错误 | `bg-red-950/80` + `text-red-200` | 保留红色警示 |

新增颜色须写入 `@theme`，扩展时保持 **黑 → 墨绿 → 翠绿** 单色系。

## 布局与间距

- 基准网格 **4px**；常用间距：`gap-1.5` `gap-2` `p-2` `p-3` `px-3 py-2`。
- 浮动控件距边缘 **`left-3 top-3` / `right-3 bottom-3`**，保持对称呼吸感。
- 面板最大宽度用 `max-w-[85%]` 等约束，防止小屏溢出。

## 组件形态

```
面板  → rounded-lg + backdrop-blur + bg-surface-900/70
药丸按钮 → rounded-full + text-xs font-medium
切换/标签 → rounded-md + 选中态 bg-brand-600 text-ink-100
数据卡   → rounded-lg + font-mono text-[11px] + text-ink-200
```

- 边框：需要时用 `ring-1 ring-brand-500/10` 或 `border border-surface-700`，勿用粗实线或纯白描边。
- 圆角：面板 `rounded-lg`，按钮 `rounded-full` 或 `rounded-md`，保持同一类组件一致。

## 交互与无障碍

- 所有可点击元素加 `pointer-events-auto`（父层 `pointer-events-none` 时）。
- 按钮必须有 `type="button"`，可见标签；装饰性标题用 `sr-only`。
- 焦点态：`focus-visible:ring-2 focus-visible:ring-brand-500/50 focus-visible:outline-none`。
- 对比度：正文在深色底上不低于 WCAG AA。

## 反模式（避免）

- 白底卡片、蓝色系强调、高饱和 unrelated 渐变。
- `slate-*` / `sky-*` / `cyan-*` 等与墨绿翠绿冲突的色系。
- 大于 `text-sm` 的正文叠在 3D 场景上。
- 无 `backdrop-blur` 的纯色大块遮罩。

## 示例

```tsx
// ✅ 墨绿玻璃面板
<div className="rounded-lg bg-surface-900/70 px-3 py-2 text-xs text-ink-300 backdrop-blur ring-1 ring-brand-500/10">
  {status}
</div>

// ✅ 翠绿选中态
<button
  type="button"
  className={`rounded-md px-2.5 py-1 text-[11px] font-medium backdrop-blur transition ${
    active
      ? "bg-brand-600 text-ink-100"
      : "bg-surface-800/70 text-ink-300 hover:bg-surface-800/90"
  }`}
/>

// ❌ 避免 — 冷灰蓝 slate、白底
<div className="bg-slate-900 text-slate-300">{status}</div>
```
